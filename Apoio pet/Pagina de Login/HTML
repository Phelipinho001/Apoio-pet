<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>ApoioPet | Login</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS externo -->
  <link rel="stylesheet" href="login.css">
</head>

<body>
  <div class="container-fluid login-wrapper">
    <div class="row g-0 row-full">

      <!-- Lado esquerdo -->
      <div class="col-12 col-md-5">
        <img src="img1.png" class="side-image">
      </div>

      <!-- Lado direito -->
      <div class="col-12 col-md-7 d-flex align-items-center justify-content-center">
        <div class="login-card">

          <img src="img2.png" class="logo-img mb-3">

          <h1 class="fw-bold">Login</h1>

          <!-- Caixa de mensagens -->
          <div id="messageBox" class="msg-box"></div>

          <!-- Botões Quero Divulgar / Quero Adotar -->
          <div class="d-flex justify-content-center gap-3 mb-4">
            <button type="button" class="btn role-btn" id="btnDivulgar" data-role="divulgar">Quero Divulgar</button>
            <button type="button" class="btn role-btn" id="btnAdotar" data-role="adotar">Quero Adotar</button>
          </div>

          <!-- Formulário -->
          <form id="loginForm" class="text-start">
            <div class="mb-3">
              <input type="text" id="email" class="form-control form-control-pill"
                placeholder="Digite seu E-mail ou Nome de Usuário">
            </div>

            <div class="mb-3">
              <input type="password" id="senha" class="form-control form-control-pill"
                placeholder="Digite sua Senha">
            </div>

            <div class="d-grid mb-3">
              <button type="submit" class="btn btn-submit">Entrar</button>
            </div>

            <div class="d-flex justify-content-between">
              <a href="#" class="link-sm">Esqueceu a senha?</a>
              <a href="register.html" class="link-sm">Não tem conta?</a>
            </div>
          </form>

          <!-- HOME SIMULADA -->
          <div id="homeView" class="home-view">
            <h2 id="homeWelcome" class="fw-bold mb-3"></h2>
            <p>Você está logado na Home da aplicação.</p>

            <button id="logoutBtn" class="btn btn-outline-secondary mt-3">Sair</button>
          </div>

        </div>
      </div>

    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- SEU SCRIPT -->
  <script src="login.js"></script>
</body>
</html>
