<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <title>ApoioPet | Cadastrar</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- CSS externo -->
  <link rel="stylesheet" href="cadastro.css">
</head>
<body>
  <div class="container-fluid">
    <div class="row g-0 row-full">

      <!-- Imagem esquerda -->
      <div class="col-12 col-md-5 side-image-wrapper">
        <img src="img1.png" alt="Cão" class="side-image">
      </div>

      <!-- Formulário -->
      <div class="col-12 col-md-7 d-flex align-items-center justify-content-center">
        <div class="card-area">

          <img src="img2.png" alt="Logo" class="logo-img mb-2">
          <h1>Cadastrar</h1>

          <div id="messageBox" class="msg-box"></div>

          <form id="registerForm" class="text-start" novalidate>
            <div class="mb-3">
              <input id="fullName" class="form-control form-control-pill" placeholder="Nome Completo" required>
            </div>

            <div class="mb-3">
              <input id="email" type="email" class="form-control form-control-pill" placeholder="E-mail" required>
            </div>

            <div class="mb-3">
              <input id="phone" class="form-control form-control-pill" placeholder="Telefone" required>
            </div>

            <div class="mb-3">
              <input id="password" type="password" class="form-control form-control-pill" placeholder="Senha" required>
            </div>

            <div class="mb-3">
              <input id="confirmPassword" type="password" class="form-control form-control-pill" placeholder="Confirme sua senha" required>
            </div>

            <div class="d-grid mb-3">
              <button type="submit" class="btn btn-entrar">Entrar</button>
            </div>

            <div class="d-grid">
              <a href="pet.html" class="btn btn-linklike">Já tenho uma Conta</a>
            </div>
          </form>

        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- JS externo -->
  <script src="cadastro.js"></script>
</body>
</html>
